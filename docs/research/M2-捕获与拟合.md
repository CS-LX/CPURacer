# M2 — 捕获与蓝线拟合

**状态：** 实现已接入；本机手工拟合验收通过（含浅/深主题冒烟，2026-08-08）。

## 选型

| 路径 | 用途 |
|---|---|
| **Screen ROI**（桌面 DC `BitBlt`） | **主路径**：合成帧，含利用率蓝线 |
| `CvChartWindow` BitBlt | **禁止**作蓝线来源（仅网格） |
| Windows Graphics Capture | **后置债**：接口可换实现；本切片未接 |

捕获使用桌面 `BitBlt(SRCCOPY)`，Overlay HWND 设置
`WDA_EXCLUDEFROMCAPTURE`；**禁止**逐帧藏窗（会导致闪烁）。详见
[M2-拟合线问题调研.md](M2-拟合线问题调研.md)。

## 高度场

- 类：`HeightFieldExtractor` + `PlotInset.DefaultWin11Cpu`（ROI 已是图矩形，默认仅守边 L4 T4 R4 B4）
- 每列寻找高饱和蓝色描边脊；缺失列由邻接轮廓插值
- 整帧可信覆盖率不足时跳过并保留上一条可靠轮廓，禁止固定高度回退
- 仅清理单列 impulse，再做轻度横向平滑（半径 1）
- 捕获刷新约 30Hz（33ms）

## 运行（验收基线）

1. **普通权限** + 托盘跟随方式 **外部 Overlay**
2. Taskmgr → 性能 → CPU，开始跟踪，开 **调试拟合线**
3. 状态含 `cap=ok cols=…`；橙色折线应贴蓝线，且不应随时间自行爬升

## 手工验收表

| 场景 | 结果 |
|---|---|
| 静止低负载平台 | 通过：橙线贴蓝线 |
| CPU 波动峰谷 | 通过：相位大体对齐 |
| 深色主题 | 通过（2026-08-08） |
| 浅色主题 | 通过（2026-08-08） |
| Alt+Tab / 遮挡 | 基线已覆盖；非本次主题冒烟重点 |

## 已知失败模式

- Overlay 使用 `WDA_EXCLUDEFROMCAPTURE` 从捕获中排除，不再逐帧隐藏
- 管理员 / 子窗跟随：见 M1.5 完整性记录；M2 拟合以普通+外部为准
- inset 不准时线整体上/下偏移 → 调 `PlotInset.DefaultWin11Cpu`
- 多显示器 DPI 混用时 ROI 可能错位（PerMonitorV2 进程仍建议单屏验收）

## 后置

- [ ] 卷速估计（帧相关）
- [ ] WGC 实现替换/并列
- [ ] 托盘暴露 inset 微调
- [ ] 量化 `|y_extract - y_visual|` 自动日志
