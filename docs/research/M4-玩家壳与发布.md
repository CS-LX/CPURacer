# M4 — 玩家壳 + 降级与发布

**状态：** ✅ 已通过（2026-08-09）— 玩家壳与开局路径  
**前置：** M3 可玩验收通过；不挖 M2.6.2 承重墙  

## 调试习惯 vs lunar UX（推翻清单）

参考：`reference/copy-dialog-lunar-lander` — `CopyWatcherApplication` 启动即 `_watcher.Start()`，托盘只有 Games / Options / Autostart / Exit；图一出现就 `Paused - click here to play`。

| 调试时代习惯（应推翻） | lunar / 玩家目标 |
|---|---|
| 托盘第一项「开始跟踪 Taskmgr」 | 启动即监视；玩家从不「开跟踪」 |
| tip 里 `roi / WGC / Ext / show / cap=` | tip 只说「打开 CPU 图 / Space 开始」 |
| 跟踪、跟随后端与开赛并列主菜单 | 主菜单：开始/停止、重开、退出；其余进高级 |
| 工程师验收路径写进 README 默认步骤 | README 默认：开程序 → 开 Taskmgr CPU → Space |
| 「请先开始跟踪」类 MessageBox | 「请打开任务管理器 CPU 性能图」 |
| 默认拟合线/红框/cap | 已关；Tab 才 debug |

**根因：** 里程碑验收把「可控开关」留成了产品默认。M4 把验收开关降到「高级」，产品路径对齐 lunar 的「装上就会自己找图」。

## 硬规则

- Play 默认：**关**拟合线、**关**调试描边；诊断只进 Advanced / Tab  
- 启动即监视 Taskmgr（可在高级里暂停）  
- 有车时不得因 `_carPose != null` 强制画红框诊断层  
- 物理仍旁路 RaceHost；不改 `TickExternalFrame` 语义  

## 验收（2026-08-09）

- [x] 默认 Play：拟合线/描边关；玩家 HUD；Tab / 高级开 debug  
- [x] 去掉「有车强制 chrome」  
- [x] idle Space 开赛；Accent 跟蓝线色  
- [x] 非管理员 UIPI 提示  
- [x] **启动自动监视**；主托盘去掉「开始跟踪」  
- [x] 托盘玩家化：开始/停止、重开、退出；监视/跟随/debug 进高级  
- [x] 托盘 tip / 失败文案玩家化  
- [x] 门禁：托盘往返后 Overlay 仍符合 M2.6.2（承袭）  

## 刻意延后（不挡 M4 关闭）

- PDH 影子山路 / CvChart ROI fallback / 自检日志 / 单文件发布  
- （可选）燃料/过热、胜负叙事、开机自启  

## 已知问题

- Taskmgr 高权限时需管理员运行，否则键可能被 UIPI 挡住  
- 捕获失败时暂无 PDH 降级地形  
- Child 跟随仍属高级项；默认 External  
